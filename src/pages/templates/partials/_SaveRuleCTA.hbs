<div class="buttons-container">
  {{#if isViewMode}}
    <a class="btn btn-link close-editor">Close</a>
  {{else}}
    <a href="#" class="btn btn-link">Close</a>
    <a class="btn save-rule">Save</a>
  {{/if}}
</div>
